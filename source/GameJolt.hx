package;
import flixel.addons.api.FlxGameJolt;

class GameJolt
{
    public function authenticateUser(username:String, userToken:String, callbackfunction:Dynamic)
        {
            authUser(username, userToken, callbackfunction)
            return initialized
        }
}